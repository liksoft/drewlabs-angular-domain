import { MapToHandlerResponse } from '../../../types';

/**
 * @description Parse login response into a {@link LoginResponse} instance
 * @param response Login response from the backend
 */
export const DrewlabsV1LoginResultHandlerFunc: MapToHandlerResponse<any> = (response: any) => null;
