# List of Tasks on dynamic inputs

- On Edit view set formgroup value, append controls for form array []
- Add simple file input when when the file control is configured to use non-multiple attributes [x]
